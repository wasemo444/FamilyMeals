services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: linknest
      POSTGRES_PASSWORD: linknest_dev
      POSTGRES_DB: linknest
    ports:
      - "55432:5432"
    volumes:
      - linknest_pgdata:/var/lib/postgresql/data

volumes:
  linknest_pgdata:
