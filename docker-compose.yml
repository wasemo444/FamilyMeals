services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mfm
      POSTGRES_PASSWORD: mfm_dev
      POSTGRES_DB: managefamilymeals
    ports:
      - "55432:5432"
    volumes:
      - mfm_pgdata:/var/lib/postgresql/data

volumes:
  mfm_pgdata:
